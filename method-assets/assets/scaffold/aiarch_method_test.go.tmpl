// Code generated by archistrator at project birth. DO NOT EDIT.
//
// This is the aiarch go-test GATE: the single `go test ./...` the aiarch-design
// workflow runs as the REQUIRED status check. methodcheck.Check is the all-in-one
// Method validator — it reads the committed .aiarch/state/project.json and enforces:
//   - the Method-invariant DESIGN rules over the committed design JSON (always);
//   - the arch LAYER rules (downward-only imports, no sideways, Temporal isolation,
//     interface naming/returns, dependency allowlist) once this repo has Go code;
//   - the design<->code ALIGNMENT pass once both a System and code are present.
// A red `go test` BLOCKS PR merge — that red check IS the trust boundary.
package method_test

import (
	"testing"

	"github.com/mixofreality-studio/archistrator-platform/framework-go/arch"
	"github.com/mixofreality-studio/archistrator-platform/framework-go/methodcheck"
)

// TestMethod is the aiarch-managed Method gate for this project. RepoRoot "." reads
// the committed .aiarch/state/project.json at the repo root; arch.MethodSpec("." ,
// "<module>/") maps this module's internal/ packages to Method layers for the layer
// rules + alignment walk (a no-op until the repo grows Go code).
func TestMethod(t *testing.T) {
	methodcheck.Check(t, methodcheck.ProjectSpec{
		RepoRoot: ".",
		Arch:     arch.MethodSpec(".", "[[.ModulePath]]/"),
	})
}
