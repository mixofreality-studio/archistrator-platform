// Code generated by cmd/modelgen from contract.schema.json; DO NOT EDIT.
// Edit the schema and re-run `make gen-models` instead.

package order

import (
	"example.com/greenfield/internal/engine/pricing"
	"example.com/greenfield/internal/resourceaccess/orderstate"
	fwm "github.com/mixofreality-studio/archistrator-platform/framework-go/manager"
	"go.temporal.io/sdk/client"
)

type Order struct {
	ID          OrderID `json:"ID"`
	AmountCents int64   `json:"AmountCents"`
	Status      string  `json:"Status"`
}

type OrderID string

// OrderManager is the generated service-contract interface for this component.
type OrderManager interface {
	PlaceOrder(rc fwm.Context, order Order) (Order, error)
}

// NewOrderManager constructs the OrderManager, delegating to the hand-written, unexported
// builder newOrderManager in the manager package (which owns the stateful facade setup:
// the Temporal client, the deps, and config). The constructor returns the
// interface, so the concrete manager impl stays unexported.
func NewOrderManager(client client.Client, orderState orderstate.OrderStateAccess, pricing pricing.PricingEngine) OrderManager {
	return newOrderManager(client, orderState, pricing)
}
