// Code generated by cmd/modelgen from contract.schema.json; DO NOT EDIT.
// Edit the schema and re-run `make gen-models` instead.

package pricing

import (
	fweng "github.com/mixofreality-studio/archistrator-platform/framework-go/engine"
)

type Order struct {
	ID          OrderID `json:"ID"`
	AmountCents int64   `json:"AmountCents"`
	Status      string  `json:"Status"`
}

type OrderID string

type PriceQuote struct {
	AmountCents int64  `json:"AmountCents"`
	Currency    string `json:"Currency"`
}

// PricingEngine is the generated service-contract interface for this component.
type PricingEngine interface {
	Price(rc fweng.Context, order Order) (PriceQuote, error)
}

// PricingEngineImpl is the generated concrete PricingEngine. Engines are pure (no
// dependencies), so the impl carries no fields and the constructor takes none.
// The interface methods are hand-written on this struct.
type PricingEngineImpl struct{}

// NewPricingEngine returns the production PricingEngine.
func NewPricingEngine() PricingEngine { return PricingEngineImpl{} }

var _ PricingEngine = PricingEngineImpl{}
