// Code generated by cmd/modelgen from contract.schema.json; DO NOT EDIT.
// Edit the schema and re-run `make gen-models` instead.

package fulfillment

import (
	fwm "github.com/mixofreality-studio/archistrator-platform/framework-go/manager"
	"go.temporal.io/sdk/client"
)

type CarrierStatus int

const (
	CarrierStatusPending   CarrierStatus = 0
	CarrierStatusInTransit CarrierStatus = 1
	CarrierStatusDelivered CarrierStatus = 2
)

type Money struct {
	AmountCents int64  `json:"AmountCents"`
	Currency    string `json:"Currency"`
}

type OrderID int64

type ShipmentID string

type ShipmentStatus struct {
	ID           ShipmentID    `json:"ID"`
	RelatedOrder OrderID       `json:"RelatedOrder"`
	Status       CarrierStatus `json:"Status"`
	Fee          Money         `json:"Fee"`
	Detail       string        `json:"Detail"`
}

// FulfillmentManager is the generated service-contract interface for this component.
type FulfillmentManager interface {
	GetShipmentStatus(rc fwm.Context, shipmentID ShipmentID, detail string) (ShipmentStatus, error)
	PingCarrier(rc fwm.Context, shipmentID ShipmentID) error
}

// NewFulfillmentManager constructs the FulfillmentManager, delegating to the hand-written, unexported
// builder newFulfillmentManager in the manager package (which owns the stateful facade setup:
// the Temporal client, the deps, and config). The constructor returns the
// interface, so the concrete manager impl stays unexported.
func NewFulfillmentManager(client client.Client) FulfillmentManager {
	return newFulfillmentManager(client)
}
